<% include includes/header %>

<!-- ABOUT US -->
	<section id="about">
		<article class="about-text">
			<p class="bold">Incredible Machines are artists and technologists who create immersive landscapes for social interaction.</p>
			<p>We make magic with a unique blend of storytelling, code, and performance. The experiences we create transport audiences to new places through cutting-edge technology, strong narrative sensibility, and a healthy dose of fun.</p>
		</article>
	</section>
	
<!-- CAPABILITIES -->
<div id="skills-section">
	<section id="skills">
			<ul>
				<% for(var i = 0; i< capabilities.length; i++){ %>
					<li><a href="#<%=makeSlug(capabilities[i].name)%>"> <%= capabilities[i].name %></a></li>
				<% } %>
			</ul>
	</section>
	
	<div id="about-skills">	

		<% for(var i = 0; i< capabilities.length; i++){ %>
		<section class="about-skill" id="<%=makeSlug(capabilities[i].name)%>">
				<article class="skill-title">
					<h2><%=capabilities[i].name %></h2>
				</article>
				<article class="skill-blurb">
					<p><%= capabilities[i].text %></p>
				</article>

	<!-- RELATED PROJECTS -->
			<article class="related-work">
				<div class="title">
					<h3>Projects</h3>
				</div>
				<div class="work-thumbs">
					<% for (var j=0; j<capabilities[i].projects.length; j++){%>
					<div class="project">
						<a href="/projects/<%= capabilities[i].projects[j].slug %>">
						<img src="<%= capabilities[i].projects[j].thumbnail.path %>" alt="Project Name" />
						<%= capabilities[i].projects[j].title %> ›</a>
					</div>
					<% } %>
				</div>
			</article>

	<!-- RELATED CLIENTS -->
			<article class="related-clients">
				<div class="title">
					<h3>Clients</h3>
				</div>
				<div class="client-list">
					<ul>

					<% var ids=[];
						for (var j=0; j<capabilities[i].projects.length; j++){
							for( var k=0; k<capabilities[i].projects[j].clients.length;k++){
								var isUsed=false;
								for(var l = 0; l < ids.length; l++){ 
									if(ids[l]==capabilities[i].projects[j].clients[k].name){
										isUsed=true;
									}
								}
								if(isUsed==false){
									ids.push(capabilities[i].projects[j].clients[k].name);%>
					<li>
						<a href="/clients"><%= capabilities[i].projects[j].clients[k].name %> ›</a>
					</li>
					<% 			}
							}		
						}%>
					</ul>
				</div>
			</article>
		</section>
		<% } %>
	</div>

	<section id="skills" class="bottom-skills">
			<ul>
				<% for(var i = 0; i< capabilities.length; i++){ %>
					<li><a href="#<%=makeSlug(capabilities[i].name)%>"> <%= capabilities[i].name %></a></li>
				<% } %>
			</ul>
	</section>


<!-- FOOTER IS INSIDE DIV TO WORK WITH CURTAINED SCROLLING-->
	<% include includes/footer %>
	</div>
		
	<% function makeSlug(Text)
{
    return Text.toLowerCase().replace(/ /g,'-').replace(/[^\w-]+/g,'');
}
%>

    <script src="/javascripts/capabilities.js"></script>

</body>
</html>